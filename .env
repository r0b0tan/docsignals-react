# Dev proxy (Vite middleware). Override in .env.local for a different endpoint.
# For production set VITE_PROXY_URL to your Cloudflare Worker URL via CI/CD secrets.
VITE_PROXY_URL=/proxy
# API key for proxy authentication (must match PROXY_API_KEY in Cloudflare Worker)
VITE_PROXY_API_KEY=
